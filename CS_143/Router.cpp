#include "Router.h"

// Add a link id to the router's link vector
void Router::addLink(std::string link_id)
{
    links.push_back(link_id);
}

// Add a link 
void Router::createRouting(<#Packet#> pkt)
{
    
}